export enum CustomNoteType {
  P2ID = "P2ID",
  P2IDR = "P2IDR",
  GIFT = "GIFT",
}

export enum NoteStatus {
  PENDING = "PENDING",
  RECALLED = "RECALLED",
  CONSUMED = "CONSUMED",
}

export enum WrappedNoteType {
  PUBLIC = "PUBLIC",
  PRIVATE = "PRIVATE",
}
